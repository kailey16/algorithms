// Given two strings, write a method to decide if one is a permutation of the other

function checkPermutation(string1, string2) {
  const st1Array = string1.split("")
  for (let i = 0; i < string2.length; i++) {
    st1Array.inclu
  }
}